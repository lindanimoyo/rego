import * as React from 'react';
import {
	View,
	TouchableOpacity,
	Text
} from "react-native";

function Account(props) {
	return(
		<>
			<View
				style={{
					backgroundColor: '#fff',
					flex:1
				}}
			>

			</View>
		</>
	)
}

export default Account;
